# 📋 План улучшений (по приоритетам)

## 🔴 КРИТИЧЕСКИЕ - Сделать первыми

### 1. Система логирования
**Почему важно:** Сейчас только print(), сложно отлаживать
**Что сделать:**
- Создать `core/logger.py` с настройкой logging
- Заменить все print() на logger
- Файлы логов с ротацией
- Уровни: DEBUG, INFO, WARNING, ERROR

**Время:** 2-3 часа

---

### 2. Конфигурационный файл
**Почему важно:** Параметры захардкожены, сложно менять
**Что сделать:**
- Создать `config.py` с настройками
- Пороги, символы, интервалы
- Валидация при запуске
- Поддержка .env файла

**Время:** 1-2 часа

---

### 3. Улучшенная фильтрация
**Почему важно:** ShitcoinAgent находит неправильные токены
**Что сделать:**
- Расширенный черный список
- Проверка по цене (стабильные ~$1)
- Проверка по имени токена
- Логирование фильтрации

**Время:** 1 час

---

## 🟡 ВАЖНЫЕ - Сделать дальше

### 4. Retry механизм для API
**Почему важно:** API могут быть недоступны
**Что сделать:**
- Декоратор retry с экспоненциальной задержкой
- Обработка rate limits
- Таймауты для запросов
- Fallback стратегии

**Время:** 2-3 часа

---

### 5. Метрики эффективности
**Почему важно:** Нужно понимать качество сигналов
**Что сделать:**
- Трекинг сигналов (отправлено/проигнорировано)
- Статистика по типам
- Простой dashboard в консоли
- Экспорт в CSV/JSON

**Время:** 3-4 часа

---

### 6. Валидация данных
**Почему важно:** Предотвратить ошибки
**Что сделать:**
- Валидаторы для всех типов данных
- Проверка диапазонов
- Санитизация входных данных
- Обработка edge cases

**Время:** 2-3 часа

---

## 🟢 ЖЕЛАТЕЛЬНЫЕ - Когда будет время

### 7. Технические индикаторы
- RSI, MACD, Bollinger Bands
- Паттерны свечей
- Volume Profile

### 8. Веб-дашборд
- Flask/FastAPI приложение
- Графики и визуализация
- История сигналов

### 9. Machine Learning
- Улучшение скоринга
- Предсказание успешности сигналов
- Адаптивные пороги

---

## 🚀 Быстрый старт улучшений

### Шаг 1: Логирование (30 минут)
```python
# core/logger.py
import logging
import os
from datetime import datetime

def setup_logger(name, log_file='logs/system.log'):
    logger = logging.getLogger(name)
    logger.setLevel(logging.INFO)
    
    # Файл
    file_handler = logging.FileHandler(log_file)
    file_handler.setFormatter(
        logging.Formatter('%(asctime)s - %(name)s - %(levelname)s - %(message)s')
    )
    
    # Консоль
    console_handler = logging.StreamHandler()
    console_handler.setFormatter(
        logging.Formatter('%(levelname)s - %(message)s')
    )
    
    logger.addHandler(file_handler)
    logger.addHandler(console_handler)
    return logger
```

### Шаг 2: Конфигурация (30 минут)
```python
# config.py
import os
from dataclasses import dataclass

@dataclass
class Config:
    # Telegram
    telegram_bot_token: str = os.getenv("TELEGRAM_BOT_TOKEN", "")
    telegram_chat_id: str = os.getenv("TELEGRAM_CHAT_ID", "")
    
    # Пороги
    whale_threshold_usd: float = 100000
    volume_spike_threshold: float = 3.0
    min_confidence: float = 0.5
    
    # Символы
    default_symbols: list = None
    
    def __post_init__(self):
        if self.default_symbols is None:
            self.default_symbols = [
                "BTCUSDT", "ETHUSDT", "BNBUSDT", ...
            ]
```

### Шаг 3: Retry декоратор (20 минут)
```python
# core/utils.py
import asyncio
from functools import wraps

def retry(max_attempts=3, delay=1, backoff=2):
    def decorator(func):
        @wraps(func)
        async def wrapper(*args, **kwargs):
            for attempt in range(max_attempts):
                try:
                    return await func(*args, **kwargs)
                except Exception as e:
                    if attempt == max_attempts - 1:
                        raise
                    wait = delay * (backoff ** attempt)
                    await asyncio.sleep(wait)
            return None
        return wrapper
    return decorator
```

---

## 📊 Ожидаемый эффект

После критических улучшений:
- ✅ Легче отлаживать проблемы
- ✅ Проще настраивать систему
- ✅ Меньше ложных сигналов
- ✅ Более стабильная работа

**Начните с логирования - это самое важное!**

