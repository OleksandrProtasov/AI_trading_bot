# Web layer

- **`api.py`** — REST API (default: `http://localhost:8001`, docs at `/docs`).
- **`dashboard_enhanced.py`** — Web UI (default: `http://localhost:8000`).

Run from the **repository root** so imports resolve:

```bash
python web/api.py
python web/dashboard_enhanced.py
```
